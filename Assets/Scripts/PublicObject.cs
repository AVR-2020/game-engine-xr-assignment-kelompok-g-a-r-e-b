using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PublicObject : MonoBehaviour
{

    public static string activeModel;
    // Start is called before the first frame update
    void Start()
    {
        activeModel = "No Object";   
    }
}
